# دليل تشغيل نظام إدارة الجمعية الخيرية على الشبكة المحلية

## 📋 المحتويات
1. [الطريقة الأولى: باستخدام Python (الأسهل)](#الطريقة-الأولى-python)
2. [الطريقة الثانية: باستخدام Node.js](#الطريقة-الثانية-nodejs)
3. [الطريقة الثالثة: باستخدام XAMPP](#الطريقة-الثالثة-xampp)
4. [ربط الحالات (المستفيدين) بالنظام](#ربط-الحالات-بالنظام)
5. [إنشاء تطبيق سطح مكتب حقيقي](#تطبيق-سطح-مكتب)

---

## الطريقة الأولى: Python (الأسهل والأسرع) {#الطريقة-الأولى-python}

### الخطوات:

#### 1. تحميل Python
- اذهب إلى: https://www.python.org/downloads/
- حمّل أحدث إصدار لنظام Windows
- أثناء التثبيت، ضع علامة ✅ على "Add Python to PATH"

#### 2. إنشاء مجلد للنظام
```
قم بإنشاء مجلد مثل:
C:\CharitySystem\
```

#### 3. وضع الملفات
- ضع ملف `charity-management.html` في المجلد
- أعد تسميته إلى `index.html`

#### 4. إنشاء ملف تشغيل السيرفر
قم بإنشاء ملف نصي جديد وأعد تسميته إلى `start-server.bat` ثم ضع فيه:

```batch
@echo off
echo ====================================
echo نظام إدارة الجمعية الخيرية
echo ====================================
echo.
echo جاري تشغيل السيرفر...
echo.

cd /d "%~dp0"

for /f "tokens=2 delims=:" %%a in ('ipconfig ^| findstr /c:"IPv4"') do set IP=%%a
set IP=%IP:~1%

echo السيرفر يعمل الآن!
echo.
echo ====================================
echo يمكنك الوصول للنظام من:
echo.
echo على هذا الجهاز:
echo http://localhost:8000
echo.
echo من الأجهزة الأخرى على نفس الشبكة:
echo http://%IP%:8000
echo ====================================
echo.
echo لإيقاف السيرفر، اضغط Ctrl+C
echo.

python -m http.server 8000

pause
```

#### 5. تشغيل النظام
- انقر مرتين على ملف `start-server.bat`
- ستظهر نافذة سوداء تعرض عنوان IP للوصول

#### 6. الوصول من أجهزة أخرى
```
مثال:
http://192.168.1.100:8000
```
استخدم العنوان الذي يظهر في النافذة السوداء

---

## الطريقة الثانية: Node.js {#الطريقة-الثانية-nodejs}

### الخطوات:

#### 1. تحميل Node.js
- اذهب إلى: https://nodejs.org/
- حمّل النسخة الموصى بها (LTS)

#### 2. تثبيت http-server
افتح Command Prompt وأكتب:
```bash
npm install -g http-server
```

#### 3. إنشاء ملف تشغيل
أنشئ ملف `start-server.bat`:

```batch
@echo off
echo نظام إدارة الجمعية الخيرية
echo ====================================
echo.

cd /d "%~dp0"

for /f "tokens=2 delims=:" %%a in ('ipconfig ^| findstr /c:"IPv4"') do set IP=%%a
set IP=%IP:~1%

echo السيرفر يعمل على:
echo http://localhost:8080
echo http://%IP%:8080
echo.

http-server -p 8080 -o

pause
```

#### 4. التشغيل
انقر مرتين على `start-server.bat`

---

## الطريقة الثالثة: XAMPP (للمحترفين) {#الطريقة-الثالثة-xampp}

### الخطوات:

#### 1. تحميل XAMPP
- اذهب إلى: https://www.apachefriends.org/
- حمّل وثبّت XAMPP

#### 2. وضع الملفات
```
ضع الملفات في:
C:\xampp\htdocs\charity\
```

#### 3. تشغيل Apache
- افتح XAMPP Control Panel
- اضغط "Start" بجانب Apache

#### 4. الوصول للنظام
```
من نفس الجهاز:
http://localhost/charity/

من أجهزة أخرى:
http://[عنوان-IP-للجهاز]/charity/
```

---

## ربط الحالات (المستفيدين) بالنظام {#ربط-الحالات-بالنظام}

### لإتاحة الوصول للمستفيدين:

#### الطريقة 1: رمز QR
يمكنك إنشاء رمز QR يحتوي على رابط النظام:

1. استخدم موقع مثل: https://www.qr-code-generator.com/
2. أدخل الرابط: `http://عنوان-IP:8000`
3. احفظ صورة الـ QR
4. اطبعها ووزعها على المستفيدين

#### الطريقة 2: روابط مختصرة
- اكتب الرابط على بطاقات
- وزعها على المستفيدين

#### الطريقة 3: واتساب
أرسل الرابط مباشرة عبر مجموعة واتساب

---

## إنشاء تطبيق سطح مكتب حقيقي {#تطبيق-سطح-مكتب}

### باستخدام Electron (موصى به):

#### 1. تثبيت الأدوات
تأكد من تثبيت Node.js أولاً، ثم:

```bash
npm install -g electron
```

#### 2. إنشاء المشروع
سنقوم بإنشاء ملفات المشروع في الخطوة التالية

---

## ⚠️ ملاحظات مهمة للأمان:

### 1. جدار الحماية (Firewall)
قد تحتاج للسماح للبرنامج بالعمل على الشبكة:
- Windows Defender سيسأل عند أول تشغيل
- اختر "السماح بالوصول"

### 2. الشبكة المحلية فقط
- النظام يعمل فقط على الشبكة المحلية (WiFi/LAN)
- لن يكون متاحاً على الإنترنت
- البيانات محفوظة على كل جهاز محلياً

### 3. النسخ الاحتياطي
- البيانات محفوظة في متصفح كل جهاز
- للنسخ الاحتياطي: استخدم ميزة "تصدير البيانات"
- احفظ نسخة من المجلد بالكامل دورياً

---

## 🔧 حل المشاكل الشائعة:

### المشكلة: لا يمكن الوصول من أجهزة أخرى

**الحل:**
1. تأكد أن الأجهزة على نفس شبكة الـ WiFi
2. أوقف جدار الحماية مؤقتاً للاختبار
3. استخدم الأمر `ipconfig` لمعرفة عنوان IP الصحيح

### المشكلة: البيانات لا تُحفظ

**الحل:**
- البيانات محفوظة في كل متصفح على حدة
- لا تستخدم الوضع الخاص (Incognito/Private)
- لا تمسح بيانات المتصفح

### المشكلة: Python غير موجود

**الحل:**
```bash
# في Command Prompt، اكتب:
python --version

# إذا ظهر خطأ، أعد تثبيت Python مع تفعيل "Add to PATH"
```

---

## 📱 الوصول من الهواتف المحمولة:

1. تأكد أن الهاتف متصل بنفس WiFi
2. افتح المتصفح (Chrome/Safari)
3. أدخل العنوان: `http://[IP-Address]:8000`
4. النظام سيعمل بشكل كامل على الموبايل

---

## 💾 مشاركة البيانات بين الأجهزة:

حالياً، كل جهاز يحفظ بياناته محلياً. للمشاركة:

### الحل المؤقت:
استخدم ميزة "تصدير البيانات" ثم استوردها على الجهاز الآخر

### الحل الاحترافي:
نحتاج لإضافة قاعدة بيانات مركزية (سيتم شرحه في الإصدار المتقدم)

---

## 🎯 الخلاصة:

**للاستخدام البسيط:**
استخدم الطريقة الأولى (Python)

**للاستخدام المتوسط:**
استخدم XAMPP

**لتطبيق سطح مكتب احترافي:**
انتظر النسخة المتقدمة مع Electron

---

هل تريد مني إنشاء:
1. ملفات Electron للتطبيق الكامل؟
2. نظام قاعدة بيانات مركزية؟
3. تطبيق موبايل مخصص؟
